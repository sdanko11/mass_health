== README

This is a rails app that reads in data from a csv and parses the data to format it correctly.
There are some sql and ActiveRecord quieries in the database file.

* ...


Please feel free to use a different markup language if you do not plan to run
<tt>rake doc:app</tt>.
